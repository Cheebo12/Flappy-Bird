using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RedSkinDestroy : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        if(choose.gg==0){
            Destroy(gameObject);
        }
    }

 
}
